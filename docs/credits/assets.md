# Asset Credits

## Sprites

| Asset | Source | License | Notes |
|-------|--------|---------|-------|
| `client/src/Presentation/Tank/Tank3D.glb` | **"FREE Lowpoly Tank"** by **MrEliptik** (the GREEN variant) | Free for personal/commercial use, **credit requested** — "Lowpoly Tank by MrEliptik". Source: <https://mreliptik.itch.io/free-lowpoly-tank-3d-model> | A stylised low-poly tank rendered **live in 3D** (not a sprite): `TankView` instances the GLB in a per-tank `SubViewport` under a fixed orthographic iso camera and composites the render into the 2D world. The model splits into a hull (`Base`) and an independently-rotating `Turret` node (with the cannon), so the turret aims in real time with no directional-frame snapping. Per-part materials: the body's `Green` material is recoloured to the team colour (green/red/blue/yellow via `TeamPalette.ApplyTeamTint`), the lights are yellow, and the tracks/wheels/bolts/cannon stay black; a grow + cull-front next-pass adds the bold cartoon outline. Supersedes all earlier tank art (the procedural cartoon sprite, the Zsky_01 Super_Tank render, the flat Kenney tank). |
| `client/src/Presentation/Arena/models/Terrain*.glb` (Brick, Crate, Steel, Mountain, Building, Bridge, Bush) | **Kenney** CC0 kits: Survival Kit (`fence-fortified` → brick/fence wall), Blaster Kit (`crate-medium`), Factory Kit (`box-large` → steel), Nature Kit (`rock_largeA`, `grass_large`), City Kit Commercial (`building-a`) | **CC0 1.0** (public domain, no attribution required) | Source: <https://kenney.nl>. The 3D arena's terrain (ADR-0017): `Terrain3DView` instances one model per cell type and **auto-fits** each to the 64-unit cell at runtime. The bare kit `.glb` ship without their colormap texture, so brick/crate/steel/mountain are given a flat colour via `ModelFit.Tint`; the city building keeps its embedded texture. Mountains are one enlarged rock spun per cell so they overlap into a mass. Water is a flat plane; sandbags an olive mound. |
| `client/src/Presentation/Arena/emblems/Emblem*.glb` (Speed, Rapid, Bounce, Spread, Pierce, Repair, Shield, Missile, Phone) | **Kenney** CC0 kits: Platformer Kit (`arrows`, `arrow`, `spring`, `heart`, `bomb`), Blaster Kit (`blaster-a`, `grenade-a`, `bullet-foam-tip` → missile), Prototype Kit (`weapon-shield`) | **CC0 1.0** (public domain, no attribution required) | Source: <https://kenney.nl>. Each powerup pickup floats a small real 3D **emblem** (auto-fitted via `ModelFit`) over a colour-tinted glow disc, spinning, so it reads as what it grants (heart = repair, shield, rocket = missile, bomb = airstrike, …). Replaces the plain glowing orb. |
| `client/src/Presentation/Arena/IsoGroundSeamless.png` | Programmer-generated placeholder (`scripts/gen_iso_ground.py`/PIL) | Public domain (trivial placeholder) | The ground tile `ArenaScene.BuildGround` lays through the iso `TileMapLayer`, themed: a flat sandy diamond with hard edges + fine noise so a field of cells tessellates **seamlessly** (no per-tile border). Deterministic. |
| `client/src/Presentation/Arena/IsoGroundDesert.png`, `IsoMountain.png` (kept), `IsoBuilding.png` source `Castle_Large` | PixVoxel Wargame pack (`isometric/terrain/Desert`, `Mountains`; `isometric/color0/Castle_Large_face0_0`) | CC0 1.0 (public domain, no attribution required) | `IsoGroundDesert` is the original bordered ground (**superseded** by the seamless tile above). `IsoMountain.png` is the single mountain block stacked by `gen_iso_mountain.py`. The **Castle** (a military fortress/tower, scaled ~1.7× to ~2.5 tanks tall) is composited by `scripts/prep_iso_building.py` → `IsoBuilding.png`. Licence snapshot: `docs/licenses/PixVoxel_Wargame-LICENSE.txt`. |
| `client/src/Presentation/Arena/IsoWater.png`, `IsoMountain.png` | PixVoxel Wargame pack (`isometric/terrain/River`, `Mountains`) | CC0 1.0 (public domain, no attribution required) | `IsoWater.png` is the flat water tile drawn by `IsoTerrainView`; `IsoMountain.png` is the single source block stacked by `gen_iso_mountain.py`. Same CC0 licence snapshot as the ground tile. |
| `client/src/Presentation/Arena/IsoBush.png`, `IsoSandbags.png`, `IsoBridge.png` | Programmer-generated placeholder (`scripts/gen_iso_nature.py`/PIL) | Public domain (trivial placeholder) | Dimensional iso art for the passable terrain: a low leafy bush clump (`BushOverlay`), a low sandbag cluster (`SandbagOverlay`), and a planked bridge deck with rails (`IsoTerrainView`). Ground contact bottom-aligned to the other tiles. Deterministic. (`IsoBridge.png` replaces the earlier flat PixVoxel Road tile.) |
| `client/src/Presentation/Arena/IsoMountainStacked.png` | Derived from PixVoxel `IsoMountain.png` by `scripts/gen_iso_mountain.py` (PIL) | CC0 1.0 (public domain, no attribution required) | The mountain a cell actually renders: the PixVoxel block stacked in tapering tiers into a tall iso peak (the single tile read as a flat bump). Deterministic; base tier bottom-aligned to the other tiles. |
| `client/src/Presentation/Arena/GroundSand.png` | Kenney "Top-down Tanks Remastered" (`tileSand1`), resized by `scripts/prep_kenney_ground.py` | CC0 1.0 (public domain, no attribution required) | Source: <https://kenney.nl/assets/top-down-tanks-remastered>. **Superseded** by the iso ground tile above (kept as the old flat-top-down ground). No attribution required either way. |
| `client/src/Presentation/Arena/PickupDisc.png` | Programmer-generated (`scripts/gen_pickup_disc.py`/PIL) | Public domain (trivial generated art) | Neutral white glowing disc with a dark coin edge; `PowerupView` tints it per `PowerupKind` via Modulate. Deterministic generator committed. (Base SDXL could not produce a clean neutral/tintable disc, so this is the "generated" pickup art.) |
| `client/src/Presentation/Projectile/KenneyBullet.png` | Kenney "Top-down Tanks Remastered" (`bulletSand2`), processed by `scripts/prep_kenney_bullet.py` | CC0 1.0 (public domain, no attribution required) | Source: <https://kenney.nl/assets/top-down-tanks-remastered>. Scaled to projectile size and rotated north→east; `ProjectileView` rotates it to the shot's travel direction. |
| `client/src/Presentation/Arena/IsoBrick0.png`, `IsoBrick1.png`, `IsoBrick2.png`, `IsoSteel.png`, `IsoCrate.png` | Programmer-generated placeholder (`scripts/gen_iso_blocks.py`/PIL) | Public domain (trivial placeholder) | The **raised iso wall blocks** drawn by `IsoTerrainView` (top diamond + two shaded front faces), depth-sorted against the tanks. Brick has three damage frames (intact/cracked/rubble); steel is a plain plate. Deterministic; base diamond bottom-aligned to the other tiles. (Building moved to the PixVoxel City sprite, above.) |
| `client/src/Presentation/Arena/Walls.png` | Programmer-generated placeholder (`scripts/gen_wall_atlas.py`/PIL) | Public domain (trivial placeholder) | **Superseded** by the iso wall blocks above (the old flat 32×32 sheared atlas; `WallGridView` retired). No attribution required either way. |
| `client/src/Presentation/Arena/previews/DesertWar.png`, `CliffsAndValleys.png` | Programmer-generated placeholder (`scripts/gen_arena_previews.py`/PIL) | Public domain (trivial placeholder) | Thumbnail previews shown in the Select Map browser (`MapSelectScene`): a dusty field with scattered blocks/tanks for "Desert War", stacked plateaus + a ramp for the upcoming "Cliffs & Valleys" (ADR-0018). Deterministic; to be replaced by real in-game screenshots. |

When a Kenney pack is integrated, snapshot its license into `docs/licenses/` and
update the affected row (Kenney license ≈ CC0, no attribution required).

## Audio

| Asset | Model / Tool | License | Prompt |
|-------|-------------|---------|--------|
| `client/audio/sfx/fire.ogg` (2.0 s) | **Stable Audio Open 1.0** via ComfyUI + `scripts/comfy_audio.py`; auto-QC'd (3 candidates, CLAP-ranked, trimmed, normalised to −1 dBFS); converted FLAC→OGG via ffmpeg | **Royalty-free / commercial use permitted** — Stability AI Stable Audio Open Non-Commercial Research Community License does **not** apply to the output audio; generated audio is free for any use including commercial | "military tank cannon firing, loud single gunshot boom, powerful muzzle blast" |
| `client/audio/sfx/explosion.ogg` (3.0 s) | Same pipeline | Same | "large explosion, tank destroyed, deep booming blast, fire and debris, battlefield" |
| `client/audio/sfx/wall_break.ogg` (2.0 s) | Same pipeline | Same | "brick wall crumbling and breaking apart, heavy rubble crashing to ground, destruction impact" |
| `client/audio/sfx/pickup.ogg` (2.0 s) | Same pipeline | Same | "item pickup collected, short positive electronic beep, power-up sound, retro arcade" |
| `client/audio/sfx/victory.ogg` (3.6 s) | Same pipeline | Same | "triumphant victory fanfare, short brass sting, military bugle call, winner announcement" |
| `client/audio/sfx/ui_click.ogg` (0.2 s) | Same pipeline | Same | "soft menu button click, brief UI tap, interface interaction sound, subtle click" |
| `client/audio/sfx/ui_hover.ogg` (0.2 s) | Derived from `ui_click.ogg` via ffmpeg (`-filter:a volume=-10dB`) — a quieter click used for mouse-hover feedback | Same | (derived) |
| `client/src/Presentation/Arena/ui/victory_bg.png` | AI-generated locally via the `assetfactory` MCP (model `sd_xl_base_1.0`, the commercial-safe SDXL base per `docs/ASSET_GENERATION.md`); prompt: "festive cartoon mobile-game victory scene, fireworks, balloons, confetti, golden sunburst, two cartoon army tanks, vibrant bold cartoon vector art". | **AI-generated**; SDXL-base output license permits commercial use (CreativeML OpenRAIL-M) | The victory-screen celebration backdrop only (fireworks/balloons/sunburst). The screen itself — ribbon title, nav pills + arrows, the ranked plaque, buttons — is composed from real UI controls at runtime (`Arena3DScene.ShowMatchOver`), so no text or layout is baked into the art. Replaced the earlier `victory_banner.png` mock-up-as-backdrop. |
| `client/src/Presentation/Arena/models/imported/**` (asset-browser imports; seeded with `kenney_blaster-kit/crate-medium.glb`) | Copy-on-place imports from the local 3D library via the map editor asset browser — Kenney CC0 and KayKit CC0 packs only (the catalogue excludes every pack without a clean license; verdicts in `docs/research/asset-browser-survey.md`) | **CC0 1.0** (Kenney / KayKit, no attribution required) | The editor copies a placed library asset into this folder so saved maps keep resolving after a clone or in an export; decorations load at runtime via `GltfDocument` (`DecorationView`). The seed sample is a copy of the already-credited Blaster Kit crate. Commit newly imported files together with the map that uses them. |
