{
  "description": "Real-time multiplayer top-down tank arena — Godot 4.6 C# client (desktop, Android, browser/WASM) backed by a Cloudflare Worker relay (Durable Objects) and Supabase progression.",
  "install": [
    "dotnet restore client/TankGame.csproj",
    "godot --headless --path client --import",
    "pnpm -C server/worker install --frozen-lockfile"
  ],
  "run": [
    "godot --path client",
    "dotnet build client/TankGame.csproj -c Debug",
    "godot --headless --path client --run-tests --quit-on-finish"
  ]
}
