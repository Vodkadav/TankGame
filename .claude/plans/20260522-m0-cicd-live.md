---
task-id: 20260522-m0-cicd-live
goal: Deliver TankGame milestone M0 — green CI/CD pipeline deploying a placeholder Godot Android APK (to GitHub Releases) + a static Cloudflare Pages landing page + a no-op Worker, with Sentry on both.
created: 2026-05-22T07:00Z
updated: 2026-05-22T08:10Z
status: in-progress
---

# M0 — CI/CD live

Ephemeral orchestration plan — canonical durable state for one `/delegate` task.
Owned and written ONLY by the main-session orchestrator. Specialists read this for
context; they never write it. Gitignored; never committed.

## How to resume

Run `/delegate continue`. Done: S0, S1, S2, S3, and Wave 1 (M0-T1/T2/T3 —
committed `9e9d7b5`, pushed to `origin/m0-cicd`).
**Resume at Wave 2** — M0-T4, T7, T10, T11, T12. Then Wave 3 (T5→T6),
Wave 4 (T8+T9), then open the single `m0-cicd`→`main` PR. Reconcile task
statuses from the slice list below before dispatching. Local checkout is
already on branch `m0-cicd`.

## Context & environment (verified 2026-05-22)

- **Source of truth:** `docs/research/development-plan.md` → M0 section (12 tickets, user-signed-off 2026-05-21).
- **Repo:** `github.com/Vodkadav/TankGame` exists, public, but the **git repository is empty** (no commits, no `main`). Local `C:\programmering\TankGame` is **not** a git repo and holds only `docs/`.
- **Branch protection:** ruleset on `main` (squash-only, no force-push) is already active — this complicates the *first* push (see S2).
- **Cloudflare/secrets:** all 6 GitHub Actions secrets verified set; Pages placeholder live; `docs/setup/cloudflare.md` pre-task is complete → M0-T6 is unblocked.
- **Toolchain:** git ✓, .NET **9** SDK (no .NET 8 — pure-C# layers still build), node 24 ✓, gh ✓. **Godot 4.x not installed** (→ S1). pnpm / wrangler / actionlint not installed (install during their tickets).
- **Agent model (ADR 0008):** `@devops`, `@platform`, `@housekeeper` are executors (can write files). `@architect`, `@i18n`, `@security` are read-only advisors — their file artifacts are committed by L1 (main session) from the advisor's spec. So T4/T7/T8/T9/T10/T11/T12 content funnels through L1; T1/T2/T3/T5/T6 run as parallel executor slices.

## Slices

### S0 — Fix agent roster (executor tier)
- **owner:** L1 (main session)
- **status:** done
- **inputs:** ADR 0001, ADR 0003; `@architect` + `@lead` consultations.
- **expected output:** `@devops`/`@platform`/`@housekeeper` hold `Write, Edit`; ADR 0008 written; approval records updated.
- **review:** G1 pass — user approved "promote the 3" 2026-05-22.
- **evidence:** 7 edits across `agents/quality-devops.md`, `gamedev-platform.md`, `meta-housekeeper.md`; `docs/adr/0008-agent-execution-capability-tiers.md` created; 3 records updated in `manifest/agent-review-records.json`; `install.ps1` ran clean (12 agents).
- **verified:** installed copies in `~/.claude/agents/` confirmed — all 3 show `tools: Read, Grep, Glob, Bash, Write, Edit` + "executor tier" body section.
- **note:** Takes effect only after Claude Code restart. Infra-repo changes are uncommitted — user to review/commit in `C:\programmering\Claude`.

### S1 — Install Godot 4.x locally
- **owner:** L1 (main session)
- **status:** done (with constraint — see Log F1)
- **inputs:** none.
- **expected output:** Godot 4.x **.NET/mono** build + matching export templates installed; `godot --version` works. (~~web export template present~~ — does not exist for the .NET build; see F1.)
- **review:** G1 pass — user pre-authorised "Claude installs Godot" 2026-05-22.
- **evidence:** winget `GodotEngine.GodotEngine.Mono` 4.6.2 installed (user scope); `godot.cmd` shim in `C:\Users\mrmac\bin` (prepended to user PATH); 27 export templates installed to `%APPDATA%\Godot\export_templates\4.6.2.stable.mono\`.
- **verified:** `godot --version` → `4.6.2.stable.mono.official.71f334935`; templates dir `version.txt` = `4.6.2.stable.mono`.
- **note:** The mono `.tpz` ships NO web/HTML5 template (android/ios/macos/linux/windows only). Godot 4.6 stable .NET cannot export to web — blocks the M0 DoD as written. See F1.

### S2 — Bootstrap git repository
- **owner:** L1 (main session)
- **status:** done
- **inputs:** empty `Vodkadav/TankGame` remote.
- **expected output:** local repo initialised, remote connected, an initial commit on `main`. PR flow viable thereafter.
- **review:** G1 pass — user chose "auto" 2026-05-22 (L1 handles the push; temp-relax the ruleset only if rejected).
- **evidence:** `git init -b main`; `.gitignore` excludes `.claude/plans/`; initial commit `ab652e2` (9 files: 8 docs + `.gitignore`); remote `origin` → `github.com/Vodkadav/TankGame`; `git push -u origin main` succeeded.
- **verified:** `gh api` — remote branch `main` exists, repo default branch `main`; ruleset 16711116 still `enforcement: active` (the empty-repo first push was exempt — the ruleset never had to be touched); local `main` tracks `origin/main`.
- **note:** The ruleset blocks direct pushes to `main` for all *subsequent* changes — M0 work must land via squash-merge PR(s); granularity TBD at the Wave-1 gate.

### S3 — Revise development-plan web→Android assumptions
- **owner:** L1 (main session) — records the user's 2026-05-22 decision
- **status:** done
- **inputs:** user decisions 2026-05-22 (keep C#; deploy Android APK to GitHub Releases; keep Pages as a landing page); Log F1.
- **expected output:** `docs/research/development-plan.md` updated so it no longer assumes Godot HTML5 web export. M0 goal/DoD, the §5 `ci.yml`/`deploy.yml` spec, and tickets M0-T5/T6/T12 revised to the Android-APK reality. M1/M3/M4 DoDs + risk R1 get an explicit **superseded** banner flagging an `@architect` web→Android revision pass due before M1 kickoff — not silently left inconsistent (doc-integrity rule).
- **review:** G1 pass — content is the user's 2026-05-22 decision; L1 slice, no dispatch.
- **evidence:** 15 edits to `docs/research/development-plan.md` — header Revision line; M0 goal + revision note; DoD; what-auto-deploys; M0-T3/T5/T6/T12 ticket rows; §3 concrete M0-T3/T5/T6; §5 `ci.yml` `client-build`; §5 `deploy.yml` `deploy-pages` + new `release-apk` + `smoke-test`; superseded banner before M1; R1 marked superseded.
- **verified:** grep of the dev plan — `export-release "Web"` / `client-web` gone; M0 + §5 now say `export-debug "Android"` / `client-apk`; remaining `HTML5` hits are only the revision notes, the superseded banner, and R1 (now marked superseded).
- **note:** Completed before Wave 1. Edits land in S2's initial commit.

### M0-T1 — Scaffold monorepo layout
- **owner:** @housekeeper
- **status:** done
- **inputs:** dev-plan M0-T1; S2 done.
- **expected output:** dirs `client/src/{Domain,GameLogic,Data,Infrastructure,Presentation}`, `client/tests/`, `server/worker/`, `server/supabase/`, `shared/protocol/`, `docs/adr/`, `docs/credits/`, `docs/licenses/`, `scripts/`, `.github/workflows/`; root `README.md`, `LICENSE` (MIT), `.gitignore`, `.editorconfig`. Test-first: `scripts/test-scaffold.ps1` (Pester) asserts every path exists.
- **review:** G1 pass — @housekeeper is the exact fit (repo-skeleton scaffolding); inputs concrete.
- **evidence:** dir tree + `.gitkeep`s, `README.md`, `LICENSE` (MIT, Daniel Machado, 2026), `.editorconfig`, expanded `.gitignore`, `scripts/test-scaffold.ps1`.
- **verified:** L1 re-ran `Invoke-Pester scripts/test-scaffold.ps1` → Passed=32 Failed=0. Integration fix: 3 redundant `.gitkeep`s removed from sibling-populated dirs, test reconciled. Committed `9e9d7b5`.
- **note:** Wave 1.

### M0-T2 — Cloudflare Worker skeleton + /healthz
- **owner:** @devops
- **status:** done
- **inputs:** dev-plan M0-T2; S2 done.
- **expected output:** `server/worker/` with `wrangler.toml` (`name = "tankgame-worker"`), `src/index.ts` routing `/healthz` → `200 ok`. Test-first: Vitest + Miniflare asserts 200.
- **review:** G1 pass — @devops owns server/infra; inputs concrete.
- **evidence:** `server/worker/` — `package.json`, `wrangler.toml` (name=tankgame-worker), `tsconfig.json`, `vitest.config.ts`, `src/index.ts` (`/healthz`→200 `ok`, else 404), `src/index.test.ts`.
- **verified:** L1 re-ran `pnpm test` in `server/worker/` → Vitest 2 passed (1 file). Committed `9e9d7b5`.
- **note:** Wave 1.

### M0-T3 — Godot 4.x C# client skeleton
- **owner:** @platform
- **status:** done
- **inputs:** dev-plan M0-T3; S1 + S2 + S3 done.
- **expected output:** `client/project.godot`, `TankGame.csproj` (Godot.NET.Sdk), `Main.tscn` (CanvasLayer + Label), `src/Presentation/MainScene.cs` printing version, `export_presets.cfg` (Android preset). Test-first: GoDotTest scene-load asserts Main loads and label starts with "TankGame".
- **review:** G1 pass — @platform owns the Godot/Android client; S1/S2/S3 satisfied.
- **evidence:** `client/` — `project.godot`, `TankGame.csproj` (Godot.NET.Sdk 4.6.2, net9.0), `Main.tscn`, `src/Presentation/MainScene.cs`, `export_presets.cfg` (Android, com.vodkadav.tankgame, arm64-v8a, empty keystore), `tests/Presentation/MainSceneTests.cs`.
- **verified:** L1 re-ran `dotnet build` (0 errors) + `godot --headless --run-tests` → GoDotTest Passed: 2 Failed: 0. Committed `9e9d7b5`.
- **note:** Wave 1.

### M0-T4 — NetArchTest layer rules
- **owner:** @architect (advisor) → L1 commits
- **status:** pending
- **inputs:** dev-plan M0-T4; M0-T3 done (namespaces exist).
- **expected output:** `client/tests/Architecture/LayerRulesTests.cs` — 5 `[Fact]`s for the layer rules + complete-coverage rule (R4). Test-first: write failing, then create empty namespaces.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 2.

### M0-T5 — CI workflow ci.yml
- **owner:** @devops
- **status:** pending
- **inputs:** revised dev-plan M0-T5/§5 (S3 done); M0-T2, M0-T3, M0-T4 done.
- **expected output:** `.github/workflows/ci.yml` — path-filtered jobs (lint, arch-test, client-test, client-build, worker-test). `client-build` exports a **Godot Android debug APK** (`godot --headless --export-debug "Android"`); the debug keystore is generated in-CI (never committed); installs JDK + Android SDK. Test-first: `actionlint` passes.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 3 (before T6).

### M0-T6 — Deploy workflow deploy.yml
- **owner:** @devops
- **status:** pending
- **inputs:** revised dev-plan M0-T6/§5 (S3 done); M0-T5 done. CF secrets already set.
- **expected output:** `.github/workflows/deploy.yml` — (a) `deploy-worker` via wrangler-action; (b) `deploy-pages` via pages-action serving a **static landing page** (`TankGame M0 — build <SHA>`, links to the latest APK) — NOT a Godot export; (c) `release-apk` — on `main` attaches the APK to a GitHub Release, on PR uploads it as an Actions artifact; (d) `smoke-test` hits Worker `/healthz` + the Pages landing page. Secrets from GitHub Actions secrets only.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 3 (after T5).

### M0-T7 — i18n bootstrap
- **owner:** @i18n (advisor) → @platform/L1 commits
- **status:** pending
- **inputs:** dev-plan M0-T7; M0-T3 done.
- **expected output:** `client/i18n/strings.csv` (`keys,en,es,dk`, row `m0.boot_label`), Godot translation import config, browser-locale autoselect; `MainScene.cs` uses `Tr()`. Test-first: GoDotTest asserts `Tr` per locale.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 2.

### M0-T8 — ADR-0001 layered architecture (TankGame)
- **owner:** @architect (advisor) → L1 commits
- **status:** pending
- **inputs:** dev-plan M0-T8; M0-T4 done. **Create `templates/adr-template.md`** first (dev-plan user-decision 5).
- **expected output:** `docs/adr/0001-layered-architecture.md` (Nygard format).
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 4. TankGame's own `docs/adr/` — distinct from the infra repo's ADRs.

### M0-T9 — ADR-0002 monorepo + CI/CD (TankGame)
- **owner:** @architect (advisor) → L1 commits
- **status:** pending
- **inputs:** dev-plan M0-T9; M0-T6 done; `templates/adr-template.md`.
- **expected output:** `docs/adr/0002-monorepo-and-cicd.md`.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 4.

### M0-T10 — Pre-commit secret-scan hook
- **owner:** @security (advisor) → @devops/@housekeeper commits
- **status:** pending
- **inputs:** dev-plan M0-T10; `sec-no-hardcoded-secrets` rule; M0-T1 done.
- **expected output:** `hooks/no-secrets-scan.sh` + `scripts/hooks/no-secrets-scan.ps1` + `scripts/install-hooks.ps1`. Test-first: Pester asserts a staged `password = "abc12345"` is rejected.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 2.

### M0-T11 — Sentry SDK in the Worker
- **owner:** @devops commits under @security spec/review
- **status:** pending
- **inputs:** dev-plan M0-T11; M0-T2 done. `SENTRY_DSN_WORKER` via `wrangler secret put`.
- **expected output:** `@sentry/cloudflare` wrapping the fetch handler. Test-first: Vitest+Miniflare `/test-throw` asserts a Sentry transport mock received the event.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 2. @security is mandatory reviewer.

### M0-T12 — Sentry SDK in the Godot C# client
- **owner:** @platform commits under @security spec/review
- **status:** pending
- **inputs:** dev-plan M0-T12; S1 + M0-T3 done. `SENTRY_DSN_CLIENT` via build-time env.
- **expected output:** `Sentry.NET` initialised in `MainScene.cs`. Test-first: GoDotTest verifies `SentrySdk.IsEnabled` after init.
- **review:**
- **evidence:**
- **verified:**
- **note:** Wave 2. @security is mandatory reviewer.

## Parallelization (per dev-plan M0 map)

- Pre-waves: S2 (git bootstrap) + S3 (dev-plan revision) — independent, run in parallel. Both must finish before Wave 1.
- Wave 1: M0-T1 (@housekeeper), M0-T2 (@devops), M0-T3 (@platform) — parallel.
- Wave 2: M0-T4, M0-T7, M0-T10, M0-T11, M0-T12 — parallel (advisor-spec tickets funnel through L1).
- Wave 3: M0-T5 then M0-T6 (@devops) — sequential.
- Wave 4: M0-T8 + M0-T9 (@architect spec → L1) — parallel.

## M0 definition of done

Worker `/healthz` → `200 ok`; CI builds a Godot **Android debug APK** that, run on the Galaxy A56, shows "TankGame M0" + build SHA; merge to `main` publishes the APK to a GitHub Release < 5 min; a PR push produces a downloadable APK artifact + a Cloudflare Pages preview < 5 min; the Pages landing page shows the build SHA and links to the latest APK; NetArchTest runs green in CI; Sentry captures a test exception from both client and Worker.

## Log

- 2026-05-22T07:00Z — created, 15 slices (S0–S2 + 12 M0 tickets).
- 2026-05-22T07:20Z — S0 done (agent roster promoted to executor tier; ADR 0008; install.ps1 clean). Awaiting Claude Code restart before S1.
- 2026-05-22T07:45Z — S1 done. **BLOCKER F1:** Godot 4.6.2 stable .NET/mono build has NO web/HTML5 export — the mono export-templates `.tpz` contains zero web templates (android/ios/macos/linux/windows only); Godot docs + issue #70796 confirm .NET web export is prototype-only, not in any stable release. The M0 DoD ("public Pages URL serves the Godot HTML5 export") is **unachievable with a C# client**. Affects M0-T3, M0-T6, M0-T12 and the DoD. Orchestration **PAUSED before S2** pending a user decision on client language vs. M0 deploy target.
- 2026-05-22T08:10Z — **F1 resolved.** User decisions: keep the C# client; M0 deploys a Godot **Android debug APK** to **GitHub Releases** (Actions artifact on PRs); keep **Cloudflare Pages** as a static landing page linking to the APK. Scope change applied: new slice **S3** (revise dev-plan web→Android), M0-T3/T5/T6 + DoD + goal amended; Parallelization adds the S2∥S3 pre-wave. Status → in-progress. 16 slices now (S0–S3 + 12 M0 tickets).
- 2026-05-22T08:25Z — **S3 done** (dev-plan revised web→Android: 15 edits + superseded banner before M1 + R1 marked superseded). **S2 done** (repo bootstrapped; initial commit `ab652e2` pushed to `main`; empty-repo first push was exempt from the ruleset — no relaxation needed). Pre-waves complete. Next: Wave 1 (M0-T1/T2/T3) — pending the Wave-1 G1 gate + user confirmation of the revised slice list and PR granularity.
- 2026-05-22T08:40Z — User confirmed: **one PR for all of M0** (branch `m0-cicd`). Branch created.
- 2026-05-22T08:55Z — **Wave 1 done & verified** (M0-T1/T2/T3). G2 reproduced: Pester 32/32, Vitest 2/2, GoDotTest 2/2. Two integration fixes folded in: `export_presets.cfg` un-ignored (it is a committed deliverable, was wrongly gitignored), `.claude/settings.local.json` ignored. Committed `9e9d7b5`. Next: Wave 2 (M0-T4, T7, T10, T11, T12).
- 2026-05-22T09:00Z — Branch `m0-cicd` pushed to `origin/m0-cicd` (backup). Session checkpointed at the Wave-1/Wave-2 boundary — user to resume Wave 2 later via `/delegate continue`.
